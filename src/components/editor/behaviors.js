export function save({ unsaved, dispatch, editorState, note }) {
}

export function _new({ unsaved, dispatch, editorState, note }) {
}


// if (unsaved) {
//     const contentState = editorState.getCurrentContent()
//     const text = contentState.getPlainText()
//     dispatch({ type: 'editor/saveNote', content: text, itemId: note.itemId })
// }


// save({ unsaved, dispatch, editorState, note })
// const itemId = Date.parse(new Date())
// dispatch({ type: 'editor/newNote', itemId })

// if (unsaved) {
//     const contentState = editorState.getCurrentContent()
//     const text = contentState.getPlainText()
// }